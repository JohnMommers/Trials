# Trials

A tiny browser Snake game built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` directly in a browser, or start a simple static server:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.
