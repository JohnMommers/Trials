# Trials
Just some trials for fun

## Molecule Enumeration

`generate_molecules.py` demonstrates how to generate all combinations of a main
structure with lists of R-groups using RDKit and SELFIES. Running the script
creates `molecules.png` with a grid of the resulting molecules.
